# 用户使用手册

## 软件介绍

Bresenham 直线绘制程序是一个基于 MFC 的图形应用程序，允许用户通过鼠标交互的方式在窗口中绘制直线。程序使用经典的 Bresenham 算法确保高效、精确的直线绘制。

## 启动程序

### 方式一：通过 Visual Studio 运行
1. 打开 `LineDrawing.sln`
2. 按 `Ctrl+F5` 启动程序（不调试）

### 方式二：直接运行可执行文件
- Debug 版本：`LineDrawing\x64\Debug\LineDrawing.exe`
- Release 版本：`LineDrawing\x64\Release\LineDrawing.exe`

## 界面说明

### 主窗口
启动程序后，你会看到：
- **白色背景**：绘图区域
- **灰色十字线**：坐标系（原点在窗口中心）
- **刻度标记**：每隔 50 像素的小刻度线

### 菜单栏
- **文件**
  - 新建 (Ctrl+N)：清空画布，开始新的绘图
  - 退出：关闭程序
- **帮助**
  - 关于：显示程序信息

## 基本操作

### 绘制直线

#### 步骤 1：按下鼠标左键
- 在绘图区域的任意位置按下鼠标左键
- 这个位置将作为直线的起点

#### 步骤 2：拖动鼠标
- 保持鼠标左键按下状态
- 移动鼠标到另一个位置
- 你会看到一条蓝色预览线跟随鼠标移动

#### 步骤 3：释放鼠标左键
- 在目标位置释放鼠标左键
- 直线被固定，蓝色直线正式绘制完成

### 绘制多条直线
- 重复上述步骤，可以绘制任意多条直线
- 每条直线独立保存，不会相互覆盖

### 清空画布
- 方式1：点击菜单"文件" → "新建"
- 方式2：按快捷键 `Ctrl+N`
- 所有已绘制的直线将被清除

## 功能特点

### 1. 实时预览
- 在拖动鼠标时，能实时看到即将绘制的直线
- 预览线随鼠标位置动态更新

### 2. 精确绘制
- 使用 Bresenham 算法确保直线精确
- 直线宽度严格为 1 个像素

### 3. 坐标系参考
- 灰色十字线标示坐标轴
- 刻度线帮助判断距离和位置

### 4. 蓝色线条
- 所有直线使用蓝色 (RGB: 0, 0, 255)
- 颜色鲜明，易于识别

## 使用技巧

### 技巧 1：绘制水平线
- 按下左键后，保持 Y 坐标不变
- 沿 X 轴方向移动鼠标

### 技巧 2：绘制垂直线
- 按下左键后，保持 X 坐标不变
- 沿 Y 轴方向移动鼠标

### 技巧 3：绘制对角线
- 从坐标原点开始
- 沿 45° 角移动鼠标
- 可以绘制完美的对角线

### 技巧 4：精确控制
- 使用刻度线作为参考
- 可以准确判断起点和终点位置

## 应用场景

### 教学演示
- 演示 Bresenham 算法的实际效果
- 展示计算机图形学的基本概念

### 简单绘图
- 快速绘制几何图形的轮廓
- 创建简单的线条图案

### 算法验证
- 验证不同斜率下的直线绘制
- 测试算法的正确性和效率

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+N | 新建（清空画布）|
| Alt+F4 | 退出程序 |

## 鼠标操作

| 操作 | 功能 |
|------|------|
| 左键按下 | 选择直线起点 |
| 左键拖动 | 预览直线 |
| 左键释放 | 完成直线绘制 |

## 注意事项

### 1. 窗口大小
- 调整窗口大小后，坐标系原点会自动调整到新的中心位置
- 已绘制的直线保持原位不变

### 2. 绘图范围
- 可以在客户区的任意位置绘制
- 超出窗口范围的部分不会显示

### 3. 线条重叠
- 可以在同一位置绘制多条直线
- 后绘制的直线会覆盖先绘制的部分

### 4. 预览效果
- 预览线是临时的
- 只有释放鼠标后才会正式保存

## 常见问题

### Q1: 为什么拖动鼠标时看不到预览线？
**A**: 确保鼠标左键保持按下状态。如果松开了左键，直线会被立即固定。

### Q2: 如何删除单条直线？
**A**: 当前版本不支持单独删除。可以使用"新建"功能清空所有直线后重新绘制。

### Q3: 可以改变直线颜色吗？
**A**: 当前版本固定使用蓝色。如需其他颜色，需要修改源代码。

### Q4: 直线看起来不够平滑？
**A**: Bresenham 算法在像素级别是最优的。如果感觉不平滑，可能是显示器分辨率或缩放设置的原因。

### Q5: 程序崩溃或无响应怎么办？
**A**: 
- 确保使用 Release 版本
- 检查是否安装了 VC++ Redistributable
- 尝试以管理员权限运行

## 性能说明

### 响应速度
- 鼠标事件响应：实时
- 直线绘制：瞬时完成
- 预览刷新：流畅无延迟

### 资源占用
- 内存：约 10-20 MB
- CPU：空闲时几乎为 0
- 绘制时：短暂峰值，立即回落

### 直线数量限制
- 理论上无限制
- 实际受内存容量限制
- 数千条直线不会影响性能

## 建议和反馈

如果你在使用过程中遇到问题或有改进建议，欢迎反馈：
- 检查项目的 Issues 页面
- 提交新的 Issue 描述问题
- 提供截图和详细步骤

## 版本信息

- **版本**：1.0
- **开发工具**：Visual Studio 2022
- **框架**：MFC
- **算法**：Bresenham 整数直线算法

---

**祝使用愉快！**
